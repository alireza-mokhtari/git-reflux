# Reflog without HEAD-speak

`git-reflux` turns `git reflog` into a human-first timeline:

- When: relative time (e.g., 2h ago)
- Where: ref/branch context
- What: action (commit/merge/rebase/etc.) and diff summary
- Who: author/committer
- Which: short SHA and decorations

